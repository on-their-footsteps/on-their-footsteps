# 📖 على خُطاهم - تطبيق سير الشخصيات الإسلامية

تطبيق تفاعلي متكامل لتعلم سير الشخصيات الإسلامية من خلال واجهة مستخدم جذابة وتجربة غنية بالمحتوى التفاعلي والقصص المصورة.

## ✨ المميزات الرئيسية

- 📚 مكتبة شاملة للشخصيات الإسلامية (الأنبياء، الصحابة، التابعين، العلماء)
- 🎨 واجهة مستخدم تفاعلية مع أنيميشن ورسوم متحركة
- 🔊 قصص صوتية بتقنية عالية الجودة
- 📱 تصميم متجاوب يعمل على جميع الأجهزة (حاسوب، لوحي، هاتف)
- 📊 نظام متكامل لمتابعة التقدم والإنجازات
- 🌙 وضع الليل والنهار التلقائي
- 🔍 بحث متقدم مع تصفية حسب الفئات والعصور
- 🌍 دعم كامل للغة العربية

## 🛠️ المتطلبات الأساسية

### الخادم (Backend)
- Python 3.10+

### الواجهة الأمامية (Frontend)
- Node.js 18+
- npm 9+

### اختياري (للميزات المتقدمة)
- Redis 7+ (للتخزين المؤقت)
- PostgreSQL 15+ (بدلاً من SQLite)

## 🚀 دليل التثبيت

### 1. استنساخ المستودع
```bash
git clone https://github.com/maysaraadmin/on-their-footsteps
cd on-their-footsteps
```

### 2. تثبيت الحزم المطلوبة
```bash
# تثبيت حزم بايثون
pip install -r requirements.txt

# تثبيت حزم الواجهة الأمامية
npm install
```

### 3. إعداد قاعدة البيانات (SQLite)

يستخدم التطبيق SQLite كقاعدة بيانات افتراضية، لذا لا تحتاج إلى تثبيت أي برامج إضافية. سيتم إنشاء ملف قاعدة البيانات تلقائيًا عند تشغيل التطبيق لأول مرة.

#### الموقع الافتراضي لملف قاعدة البيانات:
```
backend/on_their_footsteps.db
```

#### لإنشاء الجداول وتشغيل عمليات الترحيل:
```bash
# الانتقال إلى مجلد الخادم
cd backend

# تشغيل عمليات الترحيل
alembic upgrade head
```

#### ملف الإعدادات (اختياري):
ملف `.env` غير مطلوب للتشغيل الأساسي، حيث أن جميع القيم الافتراضية محددة في `config.py`. يمكنك إنشاء هذا الملف إذا أردت تغيير الإعدادات الافتراضية.

> ملاحظة: Redis اختياري للتشغيل الأساسي. إذا كنت ترغب في استخدام PostgreSQL بدلاً من SQLite، يمكنك تعديل متغير `DATABASE_URL` في ملف `.env`

### 4. تشغيل التطبيق
```bash
# تشغيل التطبيق (الخادم والواجهة معًا)
python run.py
```

### 5. الوصول إلى التطبيق
- افتح متصفح الويب وانتقل إلى http://localhost:3000
- استمتع بتجربة التطبيق!

## 🤝 المساهمة في المشروع

- إذا كنت ترغب في المساهمة في المشروع، يرجى قراءة ملف CONTRIBUTING.md
- لا تتردد في تقديم طلبات السحب (pull requests) أو تقديم تقارير الأخطاء (issues)
